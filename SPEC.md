# 여행 플래너 — 프로젝트 스펙

> 비개발자가 직접 만지면서 만든, 빌드 없는 모바일웹 여행 일정 플래너.
> 빌드 도구·번들러 없이 HTML + Tailwind CDN + Vanilla JS만으로 동작.
> "Our" 브랜드(가칭) — 솔로 사용에서 시작해 공유·협업까지 확장 예정.

---

## 1. 서비스 개요

### 무엇을 하는가
모바일 브라우저에서 여행 일정을 **블록 단위로 짜고, 시간순 자동 정렬되고, 친구에게 그대로 보낼 수 있는** 가벼운 플래너.

### 누구를 위해
- 여행 갈 때 노션/메모장에 일정 적어두기 귀찮은 사람
- 같이 가는 사람과 일정 공유가 잦은 사람
- 반려동물과 함께 여행하는 사람

### 만든 배경
사용자가 다음 주(2026-06-01~03) 제주 가족 여행을 앞두고, 직접 써볼 데모로 출발 → 점진적으로 기능 확장.

---

## 2. 핵심 기능

### 🏠 홈 (`index.html`)
- 인사말 + 누적 통계 ("지금까지 N번 다녀왔어요 · 총 X일 · ₩Y")
- **탭 네비**: ✈️ 여행 / ❤️ 즐겨찾기 (개수 뱃지)
- **연도 필터**: 데이터 있는 연도만 자동 노출
- **여행 카드 그리드**(2열, 4:5 비율, 폴라로이드 느낌)
  - 좌상단: 🏠 지역 / 👨‍👩‍👧 동행 칩
  - 우상단: 연도 배지
  - 중앙: 표지 이모지(8개 테마)
  - 하단: 제목 / 날짜 / 통계
- **+ FAB**: 새 여행 시트
- **빈 상태** 안내 메시지

### ✨ 새 여행 시트
폼 구성(친절한 라벨):
- 어떤 분위기로 떠나요? (테마 8종 — 바다·산·도시·신사·노을·눈·사막·야경)
- 여행 이름
- 언제 떠나요? / 언제 돌아와요? (시작·종료 날짜 픽커)
- 어디로 가나요? (🏠 국내 / 🌏 국외 + 자유 텍스트)
- 누구랑 가나요? (가족 / 연인 / 친구 / 기타)
- 🐾 반려동물과 함께 해요 (토글)
- 총 몇 명이에요? (스테퍼 −/+)
- 루트는 어떻게 짤까요? (✏️ 직접 / ✨ 추천으로 채워요)

### 📅 여행 상세 페이지 (`trip.html`)

**헤더**
- 표지 이모지(탭 → 설정 시트), 제목 인라인 편집
- 날짜 버튼(탭 → 설정 시트의 날짜 픽커)
- 칩: 🏠 지역 / 👨‍👩‍👧 동행 / 🐾 반려동물과 함께
- 통계 박스 3개: 총 지출(탭 → 정산 페이지) / 일정 / 일수
- 비용 카테고리 합계 줄 (🎫 / 🍽 / 🚗 / ✨)
- 헤더 배경 = 트립 테마 그라데이션

**메인 영역**
- **+ 일정 추가하기** 큰 버튼 (단일 퍼널)
- **✨ {지역}에서 가볼만한 곳** 가로 스크롤 카드(이미 추가된 곳은 자동 제외)
- Day 카드들 — Day별 헤더(이름/날짜/✎/🗑) + 일정 카드 리스트 + "+ 일정 추가"

**일정 카드 구조 (왼쪽 → 오른쪽)**
- ⠿ 드래그 핸들
- ⏰ **시간** (큰 sky-600 굵은 글씨) — 탭하면 시간 팝업
- 본문: 카테고리 칩 + 🐾 칩(있을 때) / 제목 / 설명 / 📍 위치 / 🚗 이동수단 / ₩ 비용
- ❤️ 즐겨찾기 토글

**드래그 흐름**
- 블록을 끌어다 놓으면 → **이웃 시간 중간값** 자동 추천 팝업
- 확인 → 그 시간으로 적용 & 자동 정렬
- 취소 → 원래 시간 위치로 복귀
- **항상 시간 순 정렬** (시간 없는 항목은 끝으로)

### 📝 일정 추가/편집 시트
- 어느 날? (컨텍스트 Day + "+ Day N" 토글) — 편집 모드에선 숨김
- 어떤 일정? (관광 / 맛집 / 숙소 / 기타. 이동은 픽커에서 숨김 — 이동수단 필드로 대체)
- 어디로 가요? (제목)
- 몇 시쯤? (선택)
- 위치 찍기 — Leaflet + OSM + Nominatim 주소 검색
- 여기서 뭐 할 거예요?
- 어떻게 이동해요? (자차/지하철/버스/비행기/도보/택시/배/기타 픽커 + 자유 디테일)
- 얼마 들었어요? (🎫 입장료 / 🍽 음식값 / 🚗 교통비 / ✨ 기타 4분할)
- 🐾 반려동물과 함께 갈 수 있어요 (토글)
- 지우기 / 저장

### ⚙️ 여행 설정 시트
- 표지 분위기 (테마 픽커)
- 여행 날짜 (시작·종료 → 변경 시 빈 Day 자동 정리/생성)
- 어디로 가나요? (지역)
- 반려동물과 함께 해요 (토글)
- 함께 가는 사람 + 인원 스테퍼
- 이 여행 지우기

### ⏰ 시간 팝업
- "몇 시에 갈까요?" + 시간 픽커
- 저장하면 시간 순서대로 자동 정렬

### 📅 Day 편집 팝업
- Day 이름(텍스트) + 날짜(date picker, 요일 자동 계산)

### 🗺 지도 시트 (Leaflet + OSM)
- 장소 검색 (Nominatim, 한국어)
- 지도 탭 → 핀 자동 배치 + 역지오코딩으로 주소 자동 채움
- 위경도 + 주소 텍스트 저장

### 💝 마음에 담은 곳 (`favorites.html`)
- 로즈/핑크 헤더 + 같은 탭 네비
- 필터 칩: 전체 / 카테고리별 / 🐾 반려가능(emerald)
- 카드: 카테고리·🐾 칩 / 제목 / 설명 / 📍 주소 / 🚗 이동수단 / ₩ / 출처(여행+Day)
- 카드별 공유 / 🗺 지도 보기(구글 맵)
- 상단 "📤 친구에게 공유하기" — 현재 필터에 맞는 항목 일괄 공유
- Web Share API → 클립보드 폴백

### 💰 정산 페이지 (`settle.html`)
- 에메랄드 그린 테마
- 여행 총 지출(큰 숫자)
- 몇 명이 함께 나눠 낼까요? 스테퍼 (트립 `companion.count`와 양방향 바인딩)
- **한 사람당 부담 금액** (강조 카드, 그라데이션)
- 카테고리별 한 사람 부담 + 총액 보조 표시
- ← 여행으로 돌아가요

### 📤 공유 (트립 페이지 헤더 📤 버튼)
하나의 텍스트 안에:
1. 여행 메타 (제목·날짜·지역·동행)
2. Day별 그룹화된 일정 (시간 + 카테고리 + 제목 + 설명 + 📍 주소 + 🗺 구글지도 링크 + 이동수단 + ₩ 비용)
3. 총 지출 + 1인당 금액
4. **🔗 이 여행 코스 그대로 가져가기: {URL}#import=...**

### 📥 가져오기 (홈 페이지)
- 다른 사람이 보낸 `#import=...` 링크 열면 자동 감지
- 미리보기 시트 — 표지·제목·날짜·칩·통계·Day별 일정 수
- "내 여행에 담기" → 새 ID 발급해서 복제, 트립 페이지로 이동
- 즐겨찾기는 가져오지 않음(개인 데이터)
- 손상 데이터/잘못된 링크 → 친절한 알림

---

## 3. 데이터 모델

`localStorage["travel-planner-v2"]` (단일 키, JSON)

```js
{
  trips: [
    {
      id: "abc123",
      title: "제주도 여행 🌴",
      dates: "2026.06.01 — 2026.06.03",       // 표시용
      startDate: "2026-06-01",                 // ISO, 정렬·동기화용
      endDate:   "2026-06-03",
      cover: { theme: "ocean" },                // ocean|forest|city|temple|sunset|snow|desert|night
      companion: {
        type: "family",                         // family|couple|friends|etc|""
        count: 2,
      },
      region: {
        scope: "domestic",                      // domestic|international|""
        name:  "제주도",                         // 자유 텍스트
      },
      withPet: false,                           // 트립 전체 반려동물 동반 여부
      createdAt: 1747...,
      days: [
        {
          id: "day1",
          label: "Day 1",
          date:  "6/1 (월)",                    // 표시 문자열
          items: [
            {
              id: "it1",
              category: "sight",                // sight|food|move|stay|etc (move는 호환용)
              title: "성산일출봉",
              time:  "14:00",                   // HH:MM, 비어있을 수 있음
              desc:  "정상까지 산책",
              transportMode:   "car",           // car|subway|bus|flight|walk|taxi|ferry|etc|""
              transportDetail: "50분",
              location: {
                lat: 33.4588891,
                lng: 126.9408178,
                address: "성산일출봉, ...",
              },
              costEntry:     5000,
              costFood:      0,
              costTransport: 0,
              costEtc:       0,
              petFriendly:   true,
              favorite:      false,
              favoritedAt:   null,              // toast/정렬용
            },
          ],
        },
      ],
    },
  ],
}
```

### 마이그레이션
- `v1` (단일 트립) → `v2` (배열) 자동 변환
- 구버전 단일 `cost` 필드 → 카테고리 기준으로 4버킷 분배
- 구버전 자유 텍스트 `transport` → 키워드 감지로 `transportMode` 추출 + 중복 단어 제거
- 누락된 필드 기본값 자동 채움 (`petFriendly`/`withPet`/`region`/`endDate` 등)

---

## 4. 기술 스택

- **HTML + Tailwind CSS (Play CDN)** — 빌드 없음
- **Vanilla JS** — 프레임워크 없음
- **CDN 라이브러리**
  - [SortableJS](https://github.com/SortableJS/Sortable) — 모바일 드래그앤드롭
  - [Leaflet](https://leafletjs.com/) + [OpenStreetMap](https://www.openstreetmap.org/) — 무료 지도, API 키 없음
  - [Nominatim](https://nominatim.openstreetmap.org/) — 무료 한국어 주소 검색
  - [LZ-String](https://github.com/pieroxy/lz-string) — 공유 URL 압축
- **저장소**: `localStorage` 단일 키
- **인증/백엔드**: 없음 (현재 단계)
- **호스팅**: 정적 호스팅 (Vercel/Netlify 어디든 OK), 데모는 Node 정적 서버 (포트 5174)

---

## 5. 파일 구조

```
travel-planner/
├── index.html       # 홈 (앨범 그리드 + 새 여행 + 가져오기)
├── trip.html        # 여행 상세 (일정 편집 + 지도 + 공유)
├── favorites.html   # 마음에 담은 곳
├── settle.html      # 정산
├── server.js        # 데모용 Node 정적 서버
├── .claude/
│   └── launch.json  # 미리보기 서버 설정
└── SPEC.md          # 이 문서
```

전 파일이 자기완결적 (각 HTML 파일 안에 CSS/JS 인라인).

---

## 6. 주요 사용자 흐름

### A. 새 여행 만들기
홈 → + FAB → 시트에서 폼 채우기(추천 모드 선택 가능) → 여행 떠나기 ✨ → 트립 페이지 자동 이동

### B. 일정 추가
트립 페이지 → "+ 일정 추가하기" → 시트에서 Day 선택(또는 새 Day 토글) → 카테고리·제목·시간·위치(지도)·이동·비용·반려 → 저장 → 시간순 자동 정렬

### C. 추천 코스로 자동 채우기
새 여행 시트에서 "추천으로 채워요" 선택 + 지역 매칭 → 저장 → 트립 페이지 진입 시 추천 장소가 Day 균등 분배 + 시간 자동 할당

### D. 즐겨찾기 → 공유
일정 카드 ❤️ → 마음에 담은 곳에 자동 등록 → 카테고리 필터(🐾 포함) → 친구에게 공유하기 → Web Share / 클립보드

### E. 친구가 짜놓은 코스 가져오기
친구가 트립 📤 → 텍스트 + #import= 링크 받음 → 링크 클릭 → 홈 가져오기 미리보기 → 내 여행에 담기 → 새 트립으로 복제

### F. 정산
트립 헤더 "총 지출 정산하기 ›" 탭 → 정산 페이지 → 인원 스테퍼 → 한 사람당 금액 + 카테고리별 자동 계산

---

## 7. 디자인 원칙 / 톤

- **친절 모드 카피** — "여행 시작하기"가 아니라 "여행 떠나기 ✨", "삭제"가 아니라 "지우기", "확인"이 아니라 "좋아요"
- **이모지로 카테고리 식별** — 텍스트 라벨 보조
- **시간 + 위치 + 비용** 세 가지를 가장 큼지막하게
- **자동 정렬** — 사용자가 직접 정렬 안 해도 됨
- **모바일 우선** — 데스크탑은 max-w-md 가운데 정렬

---

## 8. 다음 작업 후보 (로드맵)

### P0 잔여 (이번 사이클)
- [ ] **영수증 사진 업로드** — 일정 비용 옆에 🧾 첨부
  - `<input type="file" accept="image/*" capture="environment">` + canvas 압축(1024px JPEG 70%)
  - **IndexedDB**로 사진 저장 (localStorage 용량 한계 회피)
  - 일정 카드 썸네일 / 정산 페이지에서 영수증 모아보기
  - 예상 작업: 0.5~1일
- [ ] 모바일 폰에서 직접 띄워보기 (Vercel/Netlify에 폴더 그대로 배포)

### P0.5 (브랜드 정체성 강화)
- [ ] **카카오싱크 로그인** — 단일 사용자 클라우드 백업
  - 기기 바꿔도 데이터 유지
  - 백엔드: Firebase Auth + Firestore (가장 빠른 경로)
- [ ] **비동기 협업** — 공유 링크에 "수정 제안" 모드
  - 친구가 받아서 편집 후 다시 링크로 회신
  - 백엔드 없이 URL만으로 가능
- [ ] **사진 슬롯** — 일정에 여행 사진 첨부 (`📷 사진` 자리)
- [ ] **쇼츠 슬롯** — 여행 후 회고용 (`🎬 쇼츠` 자리)

### P1 (진짜 협업)
- [ ] **Firestore 실시간 공동 편집** — 두 사람이 동시에 편집
  - 카카오 친구 검색 (Kakao Friends API, 사업자 권한 필요)
  - 권한 모델(소유자/공동편집자)
  - 충돌 해결
  - 예상 작업: 2~4주
- [ ] **결제자 필드** (`paidBy`) — 균등분배 → 정밀 정산표
- [ ] **추천 데이터 확장** — 지역 추가 + 계절/시간대별 + 외부 데이터(공공 API)
- [ ] **다국어** — 일본/동남아 여행 시 영어 UI

### P2 (회고·콘텐츠)
- [ ] 여행 종료 후 "추억 모드" — 폴라로이드 보드, 자동 회상
- [ ] 일정 + 사진 → 쇼츠 자동 합성
- [ ] 즐겨찾기 모음을 미니 지도(전체 핀)로 보기

---

## 9. 메모

### 알려진 제약
- localStorage 용량 한계(5~10MB) — 영수증 추가 시 IndexedDB 필요
- Nominatim 무료 사용 한계 — 트래픽 많아지면 Kakao/Naver Maps로 교체
- 단일 기기 데이터 — 클라우드 백업 없으면 브라우저 데이터 삭제 시 소실
- 공유 URL이 길어질 수 있음(트립당 ~2~5KB) — 카톡/메시지에선 OK, 일부 SNS는 잘림

### 결정 사항 (Why this way)
- **백엔드 없이 시작** — 데모/검증 단계, 빠르게 만지면서 진화
- **localStorage 단일 키** — 마이그레이션 한 군데에서 처리
- **OSM/Nominatim** — API 키 발급·결제 없이 즉시 동작
- **이동 카테고리는 숨김 처리** — 데이터는 보존, 신규 선택만 막음 (호환성)
- **"Our" 브랜드** — 솔로로 시작하되, 공유·가져오기 흐름으로 함께의 의미 확보. 본격 협업은 P1

### 검증된 사용자 시나리오
- ✅ 3박 4일 제주 여행 일정 짜기 (가족 2명, 14개 일정, ₩308k)
- ✅ 친구 4명 부산 여행을 추천으로 자동 채우기 (5곳 → Day별 분배)
- ✅ 일정 드래그로 시간 옮기기 → 시간 팝업 → 정렬
- ✅ 마음에 든 맛집 ❤️ → 다른 친구에게 공유 텍스트 보내기
- ✅ 4명 정산 (₩308k → 1인당 ₩77k, 카테고리별 분배)
- ✅ 공유 링크로 트립 그대로 가져오기 (전체 데이터 복원)
